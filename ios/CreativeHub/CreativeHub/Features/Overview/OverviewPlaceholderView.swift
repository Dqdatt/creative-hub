import SwiftUI

struct OverviewPlaceholderView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Tổng quan",
            subtitle: "Shell Phase 1",
            iconName: "chart.pie.fill"
        )
    }
}
