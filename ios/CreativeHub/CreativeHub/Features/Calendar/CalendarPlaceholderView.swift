import SwiftUI

struct CalendarPlaceholderView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Lịch quay",
            subtitle: "CTA trung tâm mở tab lịch quay.",
            iconName: "calendar"
        )
    }
}
