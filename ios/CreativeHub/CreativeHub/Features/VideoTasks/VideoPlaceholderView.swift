import SwiftUI

struct VideoPlaceholderView: View {
    var body: some View {
        PlaceholderFeatureView(
            title: "Video",
            subtitle: "Danh sách video sẽ dùng dữ liệu thật ở phase sau.",
            iconName: "play.rectangle.fill"
        )
    }
}
